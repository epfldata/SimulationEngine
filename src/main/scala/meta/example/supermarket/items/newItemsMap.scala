package meta.example.supermarket.items

import scala.collection.mutable.Map

/* Populated by generate_items */
object newItemsMap {
  type itemName = String
  type goodsName = String
  val itemMap: Map[itemName, goodsName] = Map[itemName, goodsName]()
}